import styles from './styles.module.scss';

export function PreviewButton(): JSX.Element {
  return (
    <button type="button" onClick={() => {}} className={styles.previewButton}>
      Djamilson A Costa
    </button>
  );
}
